// WITH_STDLIB
// FIX: Replace with 'withIndex()'
// IGNORE_K1
fun test() {
    val element = "existing"
    val list = listOf("a", "b", "c")
    for ((i, element1) in list.withIndex()) {
        println("Index $i: $element1, also $element")
    }
}
