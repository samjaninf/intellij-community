// WITH_STDLIB
// FIX: Replace with loop over elements
// IGNORE_K1
fun test(element: String, list: List<String>) {
    for (element1 in list) {
        element1.length
        element.length
    }
}
